name: PipelineMedic Dependency Test

on:
  workflow_dispatch:

jobs:
  dependency-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install invalid dependency
        run: pip install pipelinemedic-package-that-does-not-exist-987654
