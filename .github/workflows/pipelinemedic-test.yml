name: PipelineMedic Unknown Failure Test

on:
  workflow_dispatch:

jobs:
  unknown-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run opaque CI command
        run: exit 1
