name: PipelineMedic Test

on:
  push:
    branches:
      - main

jobs:
  test-failure:
    runs-on: ubuntu-latest

    steps:
      - name: Intentional dependency failure
        run: |
          echo "module not found: requests"
          exit 1
