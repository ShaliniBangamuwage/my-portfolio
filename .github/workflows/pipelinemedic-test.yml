name: PipelineMedic Test

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: PipelineMedic failure test
        run: |
          echo "PipelineMedic CI failure test"
          exit 1
